.DS_Store
.env
.env.local
.env*.local
*.egg-info/
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
node_modules/
.next/
dist/
coverage/

# Firebase deploy build output
.firebase/

# TS incremental build cache
*.tsbuildinfo

# huge exported conversation dumps — grep only, never commit
plan/archive.txt
plan/archive/

# never let a stray npm install at the repo root become Next.js's workspace root
/package.json
/package-lock.json
/node_modules/
